# Dependencies
node_modules/

# Expo
.expo/
.expo-shared/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# macOS
.DS_Store

# Build artifacts
dist/
build/
ios/build/
android/app/build/


# deps
node_modules/

# expo
.expo/
.expo-shared/

# ios
ios/Pods/
ios/build/

# misc
.DS_Store

# Environment files (never commit secrets!)
.env
.env.local
.env.*.local

# Internal launch planning docs (not for repo history)
LAUNCH_PLAN.md
GIFT_CARD_TRANSFER_PLAN.md
.design-sync/
.design-sync/
# google-services.json is a client config, not a secret (client key restricted
# by package name) — committed so EAS Android cloud builds pick it up. The real
# secret is the FCM service-account key, which lives only in EAS credentials.
# google-services.json

MASTER_CHECKLIST_STATUS.md 
# AI agent tooling (local only — skills installed via `npx skills add`,
# symlinked per-agent; not part of the app)
.agents/
.claude/
.hermes/
skills-lock.json
